Github:
https://github.com/TryCatchLearn/DatingApp
JSONTOTS

Entity represents a table in a database and its properties represent columns in this table

  services.AddDbContext<DataContext>(opt =>
  {
    opt.UseSqlServer(config.GetConnectionString("DefaultConnection"));
  });

  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost,1433;Database=DatingDB;User Id=SA;Password=Password@1;TrustServerCertificate=True"
  }

  services.AddDbContext<DataContext>(opt =>
  {
    opt.UseSqlite(config.GetConnectionString("DefaultConnection"));
  });

  "ConnectionStrings": {
    "DefaultConnection": "Data source=dating.db"
  }